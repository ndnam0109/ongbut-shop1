import {useShopContext} from "@/lib/providers/shop-provider";

export default function CustomerLoginDialog(){
    const {shop, shopCode, loginCustomerOTP, customer} = useShopContext();


    return(
        <></>
    )
}
